name: cashmate
description: "Flutter MVP for CashMate"
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  local_auth: ^2.3.0
  shared_preferences: ^2.2.2
  image_picker: ^1.1.2
  http: ^1.6.0
  fl_chart: ^0.68.0 # <-- TAMBAHKAN LINE INI
  pdf: ^3.10.8           # <-- TAMBAHKAN INI
  path_provider: ^2.1.2
  url_launcher: ^6.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/cashmate-logo.png